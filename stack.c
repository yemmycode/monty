#include "monty.h"

/**
* f_stack - Outputs the element at the top of the stack.
* @head: A pointer to the top of the stack.
* @counter: The line number from the script where the function was called.
* Return: This function does not return a value.
*/
void f_stack(stack_t **head, unsigned int counter)
{
(void)head;

(void)counter;

bus.lifi = 0;
}
